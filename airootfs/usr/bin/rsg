#!/bin/sh
set -e
# small script to grep stuff in files in realtime, and to open it in kakoune
p=$(rg --column . | up -e rg | head -n1 | perl -pe 's/:/ +/;s/:[^:]+$//')
if [ -n "$p" ]; then
	kak $p
fi
