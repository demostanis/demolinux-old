demostanis linux

To build packages, build the ISO and test it inside QEMU:
  # make

For other make targets, check the Makefile.

Maintainance notes:
  Check if Wayfire has been updated to support sending
to Waybar only the windows in the current workspace.

